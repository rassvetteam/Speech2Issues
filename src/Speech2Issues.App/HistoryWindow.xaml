<Window x:Class="Speech2Issues.App.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="История" Width="920" Height="650" MinWidth="760" MinHeight="520"
        WindowStartupLocation="CenterOwner" WindowStyle="None" ResizeMode="CanResize"
        Background="{DynamicResource BgBrush}" Foreground="{DynamicResource TextBrush}">
    <shell:WindowChrome.WindowChrome><shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" GlassFrameThickness="0"/></shell:WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="56"/><RowDefinition Height="*"/><RowDefinition Height="190"/></Grid.RowDefinitions>
        <Border MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"><Grid Margin="22,0"><TextBlock Text="История задач" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/><Button Style="{StaticResource IconButton}" Content="&#xE8BB;" HorizontalAlignment="Right" Click="Close_Click"/></Grid></Border>
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="22,4,22,10">
            <DataGrid x:Name="HistoryGrid" AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="HistoryGrid_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Время" Binding="{Binding CreatedAt, StringFormat={}{0:dd.MM HH:mm}}" Width="115"/>
                    <DataGridTextColumn Header="Проект" Binding="{Binding Entry.Target}" Width="150"/>
                    <DataGridTextColumn Header="Статус" Binding="{Binding Entry.Status}" Width="120"/>
                    <DataGridTextColumn Header="Заголовок" Binding="{Binding Entry.Title}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="22,0,22,18">
            <Grid><Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <DataGrid x:Name="DeliveryGrid" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Платформа" Binding="{Binding Destination}" Width="120"/>
                        <DataGridTextColumn Header="Цель" Binding="{Binding Target}" Width="*"/>
                        <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="110"/>
                        <DataGridTextColumn Header="Ошибка" Binding="{Binding Error}" Width="2*"/>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0"><Button Content="Открыть ссылку" Click="OpenLink_Click"/><Button Style="{StaticResource AccentButton}" Content="Повторить ошибки" Margin="8,0,0,0" Click="Retry_Click"/></StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
